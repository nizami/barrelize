{
  "$schema": "schema.json",
  "directories": [
    {
      "path": "src",
      "include": ["**/*.ts"],
      "exclude": ["**/*.test.ts", "schema/**"],
      "order": ["cli", "cli/commands"],
      "indexFilePath": "index.ts"
    }
  ]
}
