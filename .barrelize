{
  "$schema": "schema.json",
  "barrels": [
    {
      "name": "index.ts",
      "path": "src",
      "include": ["**/*.ts"],
      "exclude": ["**/*.test.ts", "schema/**"],
      "order": ["cli", "log"]
    }
  ]
}
