{
  "$schema": "schema.json",
  "barrels": [
    {
      "path": "src",
      "include": ["**/*.ts"],
      "exclude": ["**/*.test.ts", "schema/**"],
      "order": ["cli", "log"],
      "indexFilePath": "index.ts"
    }
  ]
}
